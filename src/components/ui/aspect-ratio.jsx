'use client';

import * as AspectRatioPrimitive from '@radix-ui/react-aspect-ratio';

// Converted to plain JavaScript (JSX)
function AspectRatio(props) {
  return <AspectRatioPrimitive.Root data-slot="aspect-ratio" {...props} />;
}

export { AspectRatio };
